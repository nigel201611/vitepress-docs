image: node:22

pages:
  stage: deploy
  script:
    - corepack enable
    - corepack prepare pnpm@10 --activate
    - pnpm install
    - pnpm docs:build
    - rm -rf public
    - mv .vitepress/dist public
  artifacts:
    paths:
      - public
  only:
    - main
