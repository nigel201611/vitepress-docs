Run the standard commit workflow. First, run `git status` and `git diff` (both staged and unstaged changes) to understand what files have changed, and `git log --oneline -10` to see recent commit messages for style reference. Then show me a summary of the changes and ask me for a commit message. Once I provide the message, stage the relevant files and create the commit using `git commit -m`. Use conventional commit format (feat:, fix:, style:, refactor:, docs:, chore:, etc.). Do not use `git add -A` or `git add .` — stage specific files instead.
